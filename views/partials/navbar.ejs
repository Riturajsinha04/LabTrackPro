<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top shadow-sm border-bottom border-primary">
  <div class="container-fluid px-4">
    <a class="navbar-brand d-flex align-items-center gap-2 fw-bold" href="/">
      <i class="bi bi-box-seam-fill text-primary fs-4"></i>
      <span>LabTrack<span class="text-primary">.Pro</span></span>
    </a>
    
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarMain">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 align-items-lg-center">
        <% if (currentUser) { %>
          <% if (currentUser.role === 'admin') { %>
            <li class="nav-item">
              <a class="nav-link" href="/admin/dashboard"><i class="bi bi-speedometer2 me-1"></i> Admin Dashboard</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/admin/assets"><i class="bi bi-tools me-1"></i> Assets Catalog</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/admin/users"><i class="bi bi-people me-1"></i> User Roles</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/requester/browse"><i class="bi bi-search me-1"></i> Browse Assets</a>
            </li>
          <% } else if (currentUser.role === 'lab_incharge') { %>
            <li class="nav-item">
              <a class="nav-link" href="/labincharge/dashboard"><i class="bi bi-speedometer2 me-1"></i> Lab Dashboard</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/labincharge/requests"><i class="bi bi-inbox me-1"></i> Issue Requests</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/labincharge/issued-items"><i class="bi bi-journal-arrow-up me-1"></i> Issued Items</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/requester/browse"><i class="bi bi-search me-1"></i> Browse Catalog</a>
            </li>
          <% } else { %>
            <li class="nav-item">
              <a class="nav-link" href="/requester/browse"><i class="bi bi-grid-3x3-gap me-1"></i> Equipment Catalog</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/requester/my-requests"><i class="bi bi-clock-history me-1"></i> My Requests</a>
            </li>
          <% } %>
        <% } %>
      </ul>

      <div class="d-flex align-items-center gap-3">
        <% if (currentUser) { %>
          <div class="text-light d-flex align-items-center gap-2">
            <div class="avatar-circle bg-primary text-white fw-bold">
              <%= currentUser.name.charAt(0).toUpperCase() %>
            </div>
            <div class="lh-1">
              <div class="fw-semibold text-white small"><%= currentUser.name %></div>
              <small class="text-light opacity-75 text-capitalize text-info">
                <%= currentUser.role.replace('_', ' ') %>
                <% if (currentUser.assignedLab) { %> (<%= currentUser.assignedLab %>)<% } %>
              </small>
            </div>
          </div>
          <form action="/auth/logout" method="POST" class="d-inline">
            <button type="submit" class="btn btn-outline-light btn-sm rounded-pill px-3">
              <i class="bi bi-box-arrow-right me-1"></i> Logout
            </button>
          </form>
        <% } else { %>
          <a href="/auth/login" class="btn btn-outline-light btn-sm rounded-pill px-3">Login</a>
          <a href="/auth/register" class="btn btn-primary btn-sm rounded-pill px-3">Register</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
